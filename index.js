function add(a, b){
    return (a + b);
}
function subtract(a, b) {
    return (a - b);
}
function multiply(a, b) {
    return (a * b);
}
function divide(a, b) {
    return (a / b);
}

let number = 5;

function increment(number){
    return (number += 1);
}
function decrement(number){
    return (number -= 1);
}

let n =  parseInt(`3`, 10); 

function makeInt(n) {
    return (n);
   
}

let num = parseInt(`nonsense!`, 10);

function makeInt(num) {
    return (num);
    
}

let numb = "2.222";

function preserveDecimal(numb) {
    numb = parseFloat(numb);
    return (numb);

}

let numbe =  "0x2328";

function makeInt(numbe) {
    numbe = parseInt(numbe, 10);
    return (numbe);
   
}